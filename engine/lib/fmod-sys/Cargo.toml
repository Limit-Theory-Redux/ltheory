# NOTE: we don't use workspace references here since fmod-sys will be replaced with crates.io equivalent
[package]
name = "fmod-sys"
authors = ["Limit Theory Redux Team"]
version = "2.2.8"
edition = "2021"

[build-dependencies]
bindgen = "0"
zip-extract = "0.1.2"
ureq = "2.6.2"
